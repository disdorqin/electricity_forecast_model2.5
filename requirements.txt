# EFM3 production environment lock
#
# Validated baseline: Windows 11, Python 3.11.14, epf-2, RTX 4060 Laptop,
# CUDA 12.4.  Do not replace the local bundled TimesFM backend with the
# separately published `timesfm` package.
#
# For a CPU-only machine, install the same versions except the torch/cu124
# lines using the officially matching CPU wheel.  The data/CPU models remain
# usable, but TimeMixer and RT916 full training are not representative there.

--extra-index-url https://download.pytorch.org/whl/cu124

# Core scientific/ML stack
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.4.2
xgboost==3.2.0
catboost==1.2.10
lightgbm==4.6.0

# Deep learning (validated CUDA build)
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# I/O and utilities
pyarrow==25.0.0
pyyaml==6.0.3
joblib==1.5.3
requests==2.32.5
python-dotenv==1.2.2
pymysql==1.2.0
openpyxl==3.1.2
chinese-calendar==1.11.0
borax==4.1.3
matplotlib==3.8.4
# vmdpy is intentionally omitted: no production code imports it.

# TimesFM local PyTorch backend and xreg compatibility
huggingface_hub[cli]==0.36.2
safetensors==0.7.0
jax==0.4.30
jaxlib==0.4.30
einshape==1.0

# Optional TimesFM Flax backend is intentionally not installed by default:
# flax==0.8.4
# optax==0.2.3
# orbax-checkpoint==0.6.4
# jaxtyping==0.2.36
