# 96正式链路长期生产维护 Phase M2：资产归档清理与甲方部署

> status: CLEAN_DEPLOYMENT_ACCEPTED_LEGACY_ARCHIVE_NONBLOCKING  
> date: 2026-09-20  
> owner: formal96 production maintenance / deployment  
> precondition: Phase M1 全部验收门通过；M1 前禁止执行任何 destructive action。  
> principle: stop new writes → verify no formal reader → preserve unique state → migrate/archive → validate → targeted delete。

## 1. 阶段目标

M2 解决两个问题：历史已经很乱，以及甲方拿到后如何长期直接运行。

1. 把 `outputs/` 收敛为 production / research / archive 三个明确域；
2. 对根目录 build/temp/Agent/crawler 残留做定向治理；
3. 将 `dist/` 从 3GB+ 混合区收敛成真正 release 与可重建 build cache；
4. 形成可交付的 predictor app + model assets + mutable state + crawler release；
5. 建立备份、恢复、retention、磁盘增长、升级回滚机制；
6. 所有清理都必须能证明不影响 formal96。

## 2. 甲方生产资产三层

### 2.1 Immutable application

```text
main.py
cli/
pipelines/
runtime/
runners/
fusion/
utils/
optim/
lightGBM/
TimesFMBackend/
TimeMixer/
RT916_SpikeFusionNet/
SGDFNet/
requirements.txt
必要 scripts/server + sync
config templates
```

### 2.2 Static model assets

```text
models/LightGBM/*
models/timesFM/*
其他 manifest 声明的 required model files
```

必须生成 size / SHA256 / model_version / required-or-optional / load smoke。

### 2.3 Mutable production state

```text
data/96/model_input/...
data/96/authoritative/...
outputs/96/ledger/
outputs/96/cache/
outputs/96/runs/
outputs/96/sync/
```

`ledger/` 可 warm-start 迁移；`runs/` 是业务审计历史；`runtime/` 不属于迁移 state；数据正常由 DB/sync 建立，不把开发机全量 data 硬塞进 immutable app release。

crawler 作为独立 release，不与 predictor mutable state 混包。

## 3. `outputs/` 逐目录处理

| 当前目录 | M2目标 | 删除/迁移前置条件 |
|---|---|---|
| `outputs/96/ledger` | KEEP canonical | 永久保留；升级前备份 |
| `outputs/96/runs` | KEEP + retention | final/manifest/report 长期，中间件按策略 |
| `outputs/96/cache` | KEEP bounded | cache policy 不影响 final |
| `outputs/96/runtime` | transient | M1 后应为空或仅 active/failed TTL sandbox |
| `outputs/96/sync` | KEEP bounded | manifest/report 保留，verbose logs 可轮转 |
| `outputs/96/feature_store` | **已迁出正式域** | server remote 包→`archive/server_backtest_96/`；其余180.15MB candidate/cache/ledger/runs/smoke→`archive/legacy_96/feature_store_residual_20260919/`；当前 formal96 不存在该目录 |
| `outputs/96/legacy` | MOVE archive | reader audit=0 |
| `outputs/ledger` | KEEP | 24生产状态，不为对称强迁 |
| `outputs/24` | KEEP | 24域另行治理 |
| `outputs/ledger_96` | KEEP read-only → 后续 archive | formal reader=0；它是混合 legacy 滚动账本，仍有研究脚本引用和 2025-12-01..12-17 独有历史，不可直接删 |
| `outputs/runs_96` | ARCHIVE | 当前仅 2026-09-16 一天，formal reader=0；价值低但先保 manifest |
| `outputs/cache` | **已归档** | →`outputs/archive/legacy_96/classifier_cache_legacy/`；formal cache 为 `outputs/96/cache` |
| `outputs/unified_runs` | **已归档** | →`outputs/archive/legacy_96/unified_runs/`；formal reader=0 |
| `outputs/RT916_SpikeMarketLab` | **已归档** | →`outputs/archive/legacy_96/RT916_SpikeMarketLab/`；formal RT916 scratch 走 invocation runtime |
| `outputs/diagnostics` | KEEP local + TTL | incident pin 可例外 |
| `outputs/crawl` | KEEP source-mode dev | 与 packaged crawler runtime 分域 |
| `outputs/platform_review` | KEEP research/report | 不进 predictor release |
| `outputs/prediction_results` | **已归档** | →`outputs/archive/historical_exports/prediction_results/` |
| `outputs/archive` | KEEP read-only | 禁止生产写入 |
| `outputs/experiments` | KEEP research | 18.7GB；不进入自动 retention，可人工冷存储 |
| `outputs/_diagnose_20260703` | DELETE | 确认仍为空 |

建议 archive 结构：

```text
outputs/archive/
  legacy_sync/
  legacy_96/
    ledger_96/
    runs_96/
    feature_store/
    rt916_spikemarketlab/
    unified_runs/
    classifier_cache/
  diagnostics_history/
  historical_exports/
```

大资产若迁到外部冷存储，仓内至少保留 manifest + SHA256 + original path。

### 3.1 96点历史回测必须拆成三类证据，不做“一份 ledger 全覆盖”

2026-09-19 实盘确认：

**A. 原服务器回测/历史快照（已整理并保护）**
- 原 `outputs/96/feature_store/remote_20260101_20260814` 已于2026-09-19整体迁移为 `outputs/archive/server_backtest_96/original_server_prediction_20251218_20260814/`，内部 ledger/runs/cache/metrics/prediction_range 原样保留。
- 实盘验收：240 个日期目录，2025-12-18 → 2026-08-14；240/240 manifest、240/240 DA long、240/240 RT long；共2897个原始文件、约128.32MB。
- 新增 `SERVER_EVIDENCE_MANIFEST.json`（旧路径→新路径、角色、日期范围、关键 ledger 行数/SHA256）和 `SHA256SUMS.json`（逐文件 size+SHA256）。
- 这是 merged improved replay 的历史 `server_ledger` 来源，代表当时服务器预测基线，不允许被后续重预测覆盖；历史 replay manifest 中保留旧 source path 作为原始 provenance，不回写篡改。
- `dist/agent_artifacts/legacy_server_96/runs_96` 另有233个 daily run（2025-12-01 → 2026-07-21，约501.7MB），232天有 DA+RT prediction、201天有 final、199天有 delivery report。其 `source_file` 路径已被历史 ledger 引用，因此本轮不搬，继续作为旧交付/运行证据包保护。

**B. 后续 improved replay（必须另存，不能冒充原服务器结果）**
- `outputs/experiments/04_pipeline_audits/re_prediction_96_merged_improved_20251218_20260814_v2/ledger`
- merge manifest 明确：在 server ledger 上替换 DA `lightgbm/timesfm` + RT `timesfm`；其余模型沿用 server。
- actual 已到 2026-08-15；prediction ledger 后续又混入 2026-09-17，因此 M2 应生成一个**不可变、明确截断的 2026-01-01..2026-08-15 improved snapshot**，并保存 merge manifest/source hashes。
- 该 snapshot 用于研究/回放/比较，不直接成为 formal learner 的未审计历史。

**C. `outputs/ledger_96` legacy rolling ledger（保留但降级）**
- DA/RT prediction 各含233个日期记录，完整日实际只到 2026-07-19；相较 server snapshot，7/20..8/14 有26个完整日缺失，但 2025-12-01..12-17 有17个 server snapshot 没有的早期历史。
- AGENTS 已标记其早期输入存在 `historical-invalid-features`，因此不能晋升为生产数据源或真实精度证据。
- M2 先保只读，等所有研究脚本改为显式 archive root 后再移动；不得把它当 server snapshot 的替代品。

### 3.2 “缓存全部历史”策略

用户希望不只保最近30天。结论：**可以而且应该保全，但不要把所有历史都塞进 formal production ledger。**

建议最终形成：

```text
outputs/archive/server_backtest_96/
  original_server_prediction_20251218_20260814/   # 已落地
    ledger/
    runs/
    cache/
    metrics/
    prediction_range_2025-12-18_to_2026-08-14/
    SERVER_EVIDENCE_MANIFEST.json
    SHA256SUMS.json
  # dist/agent_artifacts/legacy_server_96 暂留原位，保护旧 final/delivery/source_file provenance
  improved_replay_20260101_20260815/
    ledger/
    merge_manifest.json
    sha256.json
  legacy_rolling_ledger/
    # outputs/ledger_96 的唯一历史/研究副本
```

而当前 `outputs/96/ledger` 继续只承担**生产状态**：可以长期累计正式每日 prediction/actual，不需要人为裁成30天；learner 自己只取最近30个 causal complete days。也就是说“ledger 可保全量历史”和“learner 只看30天”并不冲突。旧未审计历史不直接灌入 production ledger。

### 3.3 formal96 当前磁盘最终结构（2026-09-19）

经过本轮实际迁移后，`outputs/96/` 已从约503MB收敛到约42.50MB，并且只剩正式生产五目录：

```text
outputs/96/
  ledger/    # ~6.34 MB，持久生产状态
  runs/      # ~2.59 MB，daily prediction/weight/fuse/final/manifest
  cache/     # ~20.45 MB，可复用受控 cache
  runtime/   # 0 MB，当前为空；只有 active attempt 才短暂出现
  sync/      # ~12.79 MB，同步/vintage/retention 报告
```

已迁出 `outputs/96/`：
- server original evidence：约128.32MB → `archive/server_backtest_96/`；
- residual feature_store：约180.15MB → `archive/legacy_96/feature_store_residual_20260919/`；
- pre-attempt flat runtime residue：约152.54MB → `archive/diagnostics_history/pre_attempt_runtime_96_20260919/`；
- old `outputs/96/legacy/` → `archive/legacy_96/formal96_legacy_dir/`。

因此以后看到 `outputs/96/feature_store`、`outputs/96/legacy`、平铺 `asof_96_*.parquet/models_<pid>` 重新出现，都应视为回归问题（除非用户显式运行 compatibility feature_store profile）。

`outputs/ledger_96`（45.81MB）与 `outputs/runs_96`（0.18MB）已正式定性为 **legacy/research only**：新版 formal96 主链完全不用，甲方 predictor 部署也不需要携带。链路安全已解决：formal `--96` façade 对这两个 legacy root fail-closed，2026-09-19 17:56 的真实 NORMAL run 也未修改它们（`ledger_96` mtime 仍为 2026-08-03；`runs_96` 仍为 2026-09-17）。后续为减少本地占用和混淆，可在完成旧研究脚本引用登记后整体迁入 archive；这只是资产整理，不再是生产链前置任务。

## 4. 根目录定向治理

### 4.1 根目录实盘用途与处理结论（2026-09-19）

| 根目录路径 | 实盘 | 真实用途 | 结论 |
|---|---:|---|---|
| `output_96/` | 原1个 `crawler.log`，约0.071MB | **不是 CWD 随机泄漏**；旧源码模式在代码中固定写项目根 `output_96`，而 frozen EXE 固定写 `<exe目录>/output_96` | **已修并归档**：源码模式改写 `outputs/crawl/runtime_96/`；当前 v7 EXE 行为保持 `<exe目录>/output_96` 不变；根旧目录已迁入 `outputs/archive/diagnostics_history/crawler_source_root_output_96_pre_20260919/` |
| `build/` | 约173.3MB，`crawl_96_local/da_only/v3/v3_0916` | PyInstaller 默认 workpath 中间物，不被 crawler EXE 或 formal96 运行时读取 | **非链路风险，暂留**；长期应把构建命令统一指定 `--workpath dist/build_artifacts/<build-id>`，验证可复现后再归档/删除根 `build/`，否则下次 PyInstaller 仍会重生 |
| `fixtures/` | 102文件，约4.62MB | 固定 repro ledger、sample runs、seed ledger，供测试/回归 | **有用，KEEP dev/test**；不是生产 runtime，甲方 predictor release 排除 |
| `output_da_probe/` | 原2个极小 CSV | 2026-09-13 一次性 DA probe | **已归档**到 `outputs/archive/diagnostics_history/pre_m1_root_residue_20260919/output_da_probe/`；根路径已消失 |

### 4.2 M1证明不再生成后，可定向清理

- `__pycache__/`
- `.pytest_cache/`
- `.tmp_pytest/`
- `.g1_pytest_temp*/`
- 根 `.g1_*.log`
- 根 `lgbm_predict_diag.log`（2026-09-19 已归档到 diagnostics_history，writer 已修复）
- `output_da_probe/`（2026-09-19 已归档）
- 可访问 `.g1_*.log` / `.tmp_pytest/`（2026-09-19 已归档；4个历史 `.g1_pytest_temp3..6` ACL 锁死空目录仅登记，不做全仓 ACL 修改）
- `.codex_tmp/` 已完成的一次性 smoke
- `build/` PyInstaller intermediates

ACL 无法删除的 pytest temp 只处理目标目录 owner/ACL，禁止全仓递归改权限。

### 4.3 `dist/` 收敛

当前实盘约 3.4GB：
- `dist/crawler/` ~771.6MB：正式 exe + 多个历史 exe + runtime output；
- `dist/build_artifacts/` ~1.46GB：PyInstaller/venv/build scratch；
- `dist/agent_artifacts/` ~813.7MB：Agent 历史搬运包；
- `dist/archived_crawlers/` ~249MB；
- `dist/audit/` ~74MB。

目标 `dist/crawler/`：

**当前保护对象（2026-09-20复核）**：`dist/crawler/crawl_96_auto_v7.exe`，40,810,016 bytes，SHA256=`3bc8ce2a8d943cf87296d8f092b5cd62062347f5da3426de9f6471779fb11621`。release 根当前只保留这一正式 EXE，历史版本位于 `dist/crawler/archive/`。M2 任何整理都不得原地覆盖；未来若重新打包，仍必须 candidate build → 独立 smoke → hash → 晋升。

```text
dist/crawler/
  crawl_96_auto_v7.exe       # 当前唯一正式入口 / protected artifact
  README.md
  config.example.json
  db_config.example.json
  tools/                     # 明确仍支持
  archive/                   # 有上限的回滚版本
  output_96/                 # 部署 runtime；不进入 release zip
```

规则：
- `before_*.exe` 不再堆在 release 根；当前已归入 `dist/crawler/archive/`；
- release 根只允许一个 production exe；
- `build_artifacts/` 在可复现构建确认后删除/移外部 build cache；
- `agent_artifacts/` 先做 unique inventory，再冷存储或删除；
- `archived_crawlers/` 与 `crawler/archive/` 最终只保留一个回滚域；
- `config.json/db_config.json` 等 secret 永远不进 git/release zip；
- packaged crawler runtime 不得成为 predictor 隐式输入。

### 4.4 其他根目录

| 路径 | M2处理 |
|---|---|
| `deliverables/` | research deliverable，不进 predictor runtime |
| `fixtures/` | dev/test only |
| `.agents/.opencode/.github` | 开发协作资源，最小 runtime package 可排除 |
| `.claude/.workbuddy` | local-only |
| `_archive/` | 仓库保留；最小甲方 runtime 可排除 |
| `models/` | 必须随 release 或安装器提供并 hash 校验 |
| `data/` | mutable data，通过同步建立 |

## 5. Legacy reader 解除顺序

不能直接移动 `ledger_96`，因为当前仍有多份 research script 硬编码读取它。

必须：
1. 列出 `ledger_96/runs_96/feature_store/RT916_SpikeMarketLab/cache` 所有 reader；
2. 标记 formal / legacy tool / research / docs；
3. formal production reader 必须为0；
4. research 若仍保留价值，改成显式 `--ledger-root` 或 archive-root；无价值的冻结到 archive；
5. reader audit 重跑；
6. 再移动目录；
7. 移动后跑保留 research smoke + formal full regression；
8. 至少一个验收周期后才物理删除旧位置。

禁止长期用 symlink 假装没迁。

### 5.1 2026-09-20 reader audit 结果

本轮已重新扫描当前源码而不是沿用旧结论：

- **formal production reader = 0**：`main.py --96`、`pipelines/ledger_full.py`、`ledger_weight.py`、Dynamic Snapshot/FeatureView 均不读取 `outputs/ledger_96` / `outputs/runs_96`；
- `cli/parser.py` 与 `utils/output_layout.py` 仍保留 legacy profile 的默认路径映射，这是显式兼容入口，不属于 formal reader；
- 仍有价值的 legacy 工具包括 `scripts/analyze_96_ledger.py`、`scripts/metrics_96_report.py`、`scripts/rebuild_prediction_ledger_96.py`、`auto_preheat_backtest.sh` 等；
- `scripts/experiments/nnls_ab/`、`feature_store_ab/` 等仍有多处硬编码 `outputs/ledger_96`，部分甚至保存开发机绝对路径；
- 测试中仍有 legacy correspondence/preflight 引用，用于历史兼容/污染提醒，不是生产依赖。

因此当前裁决是：**生产安全已与 legacy root 解耦，但科研复现尚未解耦；本轮不移动、不删除 `ledger_96/runs_96`。** 下一步如要归档它们，先把仍需保留的 research runner 参数化，其余实验脚本直接冻结为历史，不为了“搬目录”批量重构科研代码。

## 6. 甲方 predictor release 设计

已实现 `scripts/server/build_predictor_release.py`，采用**白名单 manifest**，不是把开发仓压缩过去。默认只 dry-run；`--apply` 才物化候选目录，且拒绝覆盖非空旧 release。

当前 release contract：

```text
formal96_predictor_release_v1

predictor/
  main.py
  cli/
  pipelines/
  runtime/
  runners/
  fusion/
  utils/
  optim/
  lightGBM/
  TimesFMBackend/
  TimeMixer/
  RT916_SpikeFusionNet/
  SGDFNet/
  models/LightGBM/
  models/timesFM/
  scripts/sync/
  scripts/server/
  requirements.txt
  .env.example
  README.md
  docs/{RUNBOOK,DATA_CONTRACT_96,LEAKAGE_AUDIT_96,PROJECT_LAYOUT,OUTPUT_CONVENTION}.md
  release_manifest.json       # 仅 --apply 后生成，逐文件 size + SHA256
```

2026-09-20 真实开发仓 dry-run：

```text
FILES=187
TOTAL_GIB=0.866
MUTABLE_STATE_INCLUDED=false
CRAWLER_INCLUDED=false
```

显式排除：`outputs/`、`data/`、`outputs/experiments`、`deliverables`、`fixtures`、`_archive`、`dist`、`build`、`scripts/tests`、`scripts/experiments`、`scripts/crawler`、`ExtremPriceClf`、Agent tooling、`.env` 和 crawler secret config。formal96 当前四阶段不需要 ExtremePriceClf；crawler 继续作为独立 release。

命令：

```powershell
# 快速白名单检查，不算 hash、不写文件
python scripts/server/build_predictor_release.py --skip-hash

# 完整 dry-run：计算实际 release 的 SHA256，但仍不写 candidate
python scripts/server/build_predictor_release.py

# 真正物化时必须使用新的候选目录，禁止覆盖旧 release
python scripts/server/build_predictor_release.py --apply --output-dir <NEW_CANDIDATE_DIR>
```

mutable state 仍单独迁移/恢复。production ledger 只能通过现有 `bootstrap_96_production_ledger.py` 审计迁入；不要把开发机 `outputs/96` 整棵塞进 immutable app release。

## 7. 部署 doctor / 机械验收

已实现 `scripts/server/doctor_96_deployment.py`。默认只读；只有显式 `--check-writable` 才在 `outputs/96/runtime` 建立一个极小探针并立即删除。

标准部署前检查：

```powershell
python scripts/server/doctor_96_deployment.py \
  --require-cuda \
  --check-db \
  --check-writable \
  --target-date YYYY-MM-DD \
  --check-final
```

物化最小 release 后增加：

```powershell
python scripts/server/doctor_96_deployment.py \
  --root <PREDICTOR_RELEASE> \
  --strict-release \
  --require-cuda
```

`--strict-release` 会要求 `release_manifest.json` 并逐文件验证 size/SHA256，同时拒绝 experiments、crawler、tests、build、archive、Agent tooling、ExtremePriceClf 和 secrets 混入 predictor。

2026-09-20 在当前真实环境的完整 doctor 已 PASS：

- Python 3.11.14；
- Torch 2.6.0+cu124 + RTX 4060 Laptop GPU；
- 8 个 production module 从当前部署根正确 import；
- required static model assets = 925,543,847 bytes；
- MySQL 5.7.40，`epf_pmos_96_full` = 165,600 rows，2022-01-01..2026-09-21；
- runtime create/write/delete PASS；
- T=2026-09-20 DA/RT adaptive ledger readiness 各30日；
- final submission/postflight PASS。

### 7.1 干净 candidate 真实部署验收（2026-09-20）

本轮已经完成该门，不再是待办。流程没有复制开发仓 `data/outputs/cache/experiments`：

1. `build_predictor_release.py --apply` 物化全新 candidate：187 files，929,549,369 bytes；
2. `release_manifest.json` 对187/187文件记录 size+SHA256；predictor 不含 mutable state、crawler、tests、experiments、build、Agent tooling、ExtremePriceClf、`.env`；
3. `bootstrap_96_production_ledger.py --apply` 从当前 production ledger 迁入 DA/RT 各30个 adaptive complete days，staging/final readiness 均 PASS；
4. strict deployment doctor：0 failure / 0 warning；TimesFM 明确解析到 candidate 自身 `models/timesFM`；DB=MySQL 5.7.40，165600 rows；CUDA=RTX 4060；runtime writable；
5. 在 candidate 根直接执行唯一正式入口 `python main.py --96 2026-09-20`：DB full sync 成功；7个真实模型腿全部实际执行并各96点；Stage2 weight、Stage3 fuse、Stage4 final 全部完成；`delivery=NORMAL`、postflight PASS、next-day readiness PASS、fallback=false；
6. candidate 内 `audit_96_artifacts.py` PASS；strict doctor + `--check-final` PASS；NORMAL 后 `outputs/96/runtime` 清空。

关键产物：DA weights=9、RT weights=12、DA/RT fused=96/96、submission=96/96；SGDFNet `anchor_source_day=2026-09-19`、`anchor_source_type=decision_day_da`、`anchor_rows=96`、`fallback_used=0`；统一 `snapshot_id=887819eb477b073ad3db735a5a20a29e91b01b61010d057f432255e7be1ef068`。

验收 hash（临时 candidate，仅作本轮审计记录）：`release_manifest.json=84b35bf33f49f7f19c7853c76cd3f2524f7d4e925455accb732e39037e2e8487`；`bootstrap_manifest.json=41ad23df0c7b3c6830ed42b6db4715f21f5b9d7d4f1074474d15ff9d157809b8`；final run manifest=`8b1309814ba30405f9888786e92a0ee01e0e6551df352533e67edb7655fabfaa`。

首个干净 candidate 还真实暴露了旧 TimesFM `PROJECT_ROOT` 路径污染；当前源码已改成 deployment-root 相对模型路径，strict doctor 新增 `timesfm_model_resolution` 硬门，candidate v2 验收已证明从自身 `models/timesFM/model.safetensors` 本地加载，不再联网/回跳开发机路径。

**生产部署门已解除。** `ledger_96/runs_96` 的研究 reader 参数化、旧 build/Agent 资产冷存储、destructive retention apply 继续作为非阻塞资产治理，不影响甲方 predictor 上线。

甲方部署不得依赖“把开发机整个目录压缩过去”。

## 8. Retention

- ledger：persistent；
- final submission / run_manifest / delivery report：长期；
- success prediction/weight/fuse intermediate：30天；
- normal logs：30天；
- failed diagnostics：90天；
- orphan runtime scratch：24小时，incident pin 除外；
- experiments：手工科研归档。

destructive retention 只有在 M2 server acceptance 后才能打开。第一次 apply 必须 dry-run → 人工审核 → ledger backup → apply → formal smoke → artifact audit → final/ledger hash 对账。

## 9. 备份与升级

每次代码/模型升级前记录：git commit、model SHA256、`outputs/96/ledger` snapshot、最近 NORMAL final + manifest、数据 cutoff/version。

回滚必须匹配 code + models + config + ledger schema/version + output contract；schema 不兼容必须 migration，禁止覆盖旧 ledger。

## 10. 单目录清理 ticket 格式

```text
ASSET=
CURRENT_SIZE=
WRITERS=
READERS=
UNIQUE_CONTENT=
ARCHIVE_TARGET=
HASH_MANIFEST=
MIGRATION=
PRE_DELETE_TEST=
DELETE_ALLOWED=YES/NO
POST_DELETE_TEST=
ROLLBACK=
```

严禁：`rm -rf outputs/*`、`git clean -fdx`、批量 restore、全仓 ACL 修改。

## 11. M2 最终验收

1. 项目根无 runtime 垃圾；
2. `outputs/` formal/legacy/research 边界清晰；
3. formal96 只依赖 `outputs/96/{ledger,runs,cache,runtime,sync}`；
4. `outputs/96/runtime` 正常运行后为空或仅 active attempt；
5. release 不含 experiments/build/Agent历史；
6. state bundle 可迁移到干净环境；
7. clean deployment + warm-start + DB sync 后一条 `python main.py --96 DATE` 可 NORMAL；
8. restart/interruption/repeat 均幂等；
9. retention dry-run/apply 通过；
10. disk usage 有上限策略；
11. README/RUNBOOK/PROJECT_LAYOUT/OUTPUT_CONVENTION/AGENTS/skills 一致；
12. formal96 full regression、artifact audit、py_compile、diff check PASS。

## 12. 最终目标

甲方长期生产只需要理解六类资产：

```text
源码（可版本化）
+ 模型资产（可校验）
+ 数据同步（可重建）
+ ledger state（可迁移）
+ daily run/final（可审计）
+ runtime（可自动回收）
```

任何无法归入以上六类的生产文件，都应视为新的治理问题，而不是继续堆在根目录。